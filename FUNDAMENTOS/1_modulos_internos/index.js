const primeiro_modulo = require("./primeiro_modulo");
const soma = primeiro_modulo.soma;
const sub = primeiro_modulo.sub;
const mult = primeiro_modulo.mult;
const div = primeiro_modulo.div;

soma(1, 2);
sub(10, 5);
mult(8, 10);
div(6, 6);
